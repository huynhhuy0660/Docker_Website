# Bỏ qua thư mục chứa thư viện (nếu bạn dùng Composer cho PHP)
/src/vendor/

# Bỏ qua các file cấu hình hệ thống của hệ điều hành
.DS_Store
Thumbs.db

# Bỏ qua các file môi trường chứa thông tin nhạy cảm (nếu có)
.env
.env.local

# Bỏ qua các file log ghi lại lỗi
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# (Tùy chọn) Bỏ qua thư mục hình ảnh nếu bạn có quá nhiều ảnh nặng, 
# nhưng thường đồ án thì cứ để hình ảnh mẫu lên cho đầy đủ.
# /src/images/